{
  "type": "module",
  "devDependencies": {
    "@types/node": "^25.0.6",
    "esbuild": "^0.27.1",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "esbuild script.ts --bundle --outfile=script.js --platform=node --format=esm",
    "run": "deno run  --allow-read --allow-write script.ts",
    "lint": "deno lint script.ts"
  }
}
